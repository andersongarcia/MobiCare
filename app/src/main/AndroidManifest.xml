<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.edu.ifspsaocarlos.sdm.cuidador">

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppMaterialTheme">
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.MenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ContatosActivity" />
        <activity android:name=".activities.ProgramasActivity" />
        <activity
            android:name=".activities.AgendaMedicacaoActivity"
            android:label="@string/title_activity_agenda_medicacao" />
        <activity android:name=".activities.CadastroUsuarioActivity" />
        <activity android:name=".activities.CadastroIdosoActivity"></activity>
    </application>

</manifest>